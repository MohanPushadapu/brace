import '../models/sensor_data.dart';

class PacketParser {
  SensorData parse(List<int> packet) {
    throw UnimplementedError('Packet parsing will be implemented after protocol verification.');
  }
}
